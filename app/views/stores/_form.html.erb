<%= form_for(store) do |f| %>
  <% if store.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(store.errors.count, "error") %> prohibited this store from being saved:</h2>

      <ul>
      <% store.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :s_name %>
    <%= f.text_field :s_name %>
  </div>

  <div class="field">
    <%= f.label :s_address %>
    <%= f.text_field :s_address %>
  </div>

  <div class="field">
    <%= f.label :s_location %>
    <%= f.text_field :s_location %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
